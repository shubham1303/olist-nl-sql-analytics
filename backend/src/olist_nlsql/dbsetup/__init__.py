"""Local database tooling: download the Olist dataset, build raw and analytics schemas.

Run ``python -m olist_nlsql.dbsetup --help``. This is development and ops tooling;
the application itself only reads through ``olist_nlsql.db``.
"""
